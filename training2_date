require 'date'

def express_date(year,month)

  day = Date.new(year,month,-1)
  month_last_day = day.day
  current_day = 1

  while month_last_day <= current_day
    puts  "#{year}年#{month}月#{current_day}日"
    current_day += 1
  end

  express_date(2000,2)

